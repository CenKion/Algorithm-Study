//numpy中数据类型默认为float64
//相同尺寸数组可直接进行四则运算。


import numpy as np
arr1=np.array([1,2,3])		//创建数组
arr2=np.zeros((3,2))		//3行2列的全0数组
arr3.shape				//获取尺寸。(3,2)
arr4=np.ones((3,2))		//全是1的数组
arr5=np.arange(3,7)		//递增数组。左闭右开。[3,4,5,6]
arr6=np.linspace(0,1,5)	//某区间等间距的数。array([0.,0.25,0.5,0.75,1.])，5为样本数
arr7=np.random.rand(2,4)	//2行4列，范围在0-1的随机数组
arr7.dtype				//获取数据类型。输出float64
arr8=np.zeros((2,4),dtype=np.int32)	//类型设为int32。其余如bool,str,np.uint32。
arr9=arr2.astyle(int)		//改类型为int
x_train = np.array([0., 1, 2, 3, 4, 5],dtype=np.longdouble)	//longdouble类型向量
-------------------------------------------------------------------------------------------------
a=np.array([1,2,3])
b=np.array([4,5,6])
np.dot(a,b)			//向量点乘，输出32
-------------------------------------------------------------------------------------------------
a2=np.array([[1,2],
             [3,4]])
b2=np.array([[2,0],
             [0,2]])
print(a2@b2)			//矩阵乘法。输出[[2 4]
 								  [6 8]]
-------------------------------------------------------------------------------------------------
a=np.array([1,2,3])
np.sqrt(a)				//元素求平方根。[1.         1.41421356 1.73205081]
np.sin(a)				//元素求正弦。[0.84147098 0.90929743 0.14112001]
np.cos(a)				//求余弦
np.log(a)				//取对，底数为e
np.power(a,2)			//指数
-------------------------------------------------------------------------------------------------
a=np.array([[1],
           [10],
           [20]])
b=np.array([0,1,2])
print(a+b)			//不同尺寸时，会先拓展到相同尺寸。输出[[ 1  2  3]
 													[10 11 12]
													 [20 21 22]]
-------------------------------------------------------------------------------------------------
a=np.array([1,2,3,4,5])
print(a.min())			//最小值，为1
print(a.max())
print(a.argmin())		//最小值索引，为0
print(a.argmax())

a=np.array([[1,2],
           [10,11],
           [20,21]])
print(a.min())			//1
print(a.max())			//21
print(a.argmin())		//0
print(a.argmax())		//5
-------------------------------------------------------------------------------------------------
a=np.array([1,2,3,4,5])
a.sum()				//所有元素总和。15
a.mean()				//平均数。3.0
np.median(a)			//中位数。3.0
a.var()				//方差。2.0
a.std()				//标准差。1.4142135623730951
np.ptp(X_train,axis=0)	//列极差
np.exp(X)				//e的x次方
np.std(X,axis=0)			//每列标准差
np.mean(X,axis=0)		//每列均值
-------------------------------------------------------------------------------------------------
a=np.array([[1,2],
           [10,11],
           [20,21]])
a.sum(axis=0)			//按列相加。[31 34]
a.mean(axis=0)			//[10.33333333 11.33333333]
np.median(a,axis=0)		//[10. 11.]
a.var(axis=0)			//[60.22222222 60.22222222]
a.std(axis=0)			//[7.76029782 7.76029782]
-------------------------------------------------------------------------------------------------
a=np.array([[1,2,3,4],
           [10,11,12,13],
           [20,21,22,23]])
print(a[0,1])			//第0行第1列。2
print(a[a<11])			//条件筛选元素。[ 1  2  3  4 10]
print(a[0,0:2])			//第0行，0-2左闭右开。[1 2]
print(a[0,0:4:2])			//第0行，0-4左闭右开，步长2。[1 3]
-------------------------------------------------------------------------------------------------
np.arange(10).reshape(-1,5)		//reshape重组。-1表示自动计算维度
np.reshape(np.arange(10),-1,5)
np.c下划(x,x**2,x**3)				//按列拼接。行则为np.r_
-------------------------------------------------------------------------------------------------
np.tile(X,(1000,1))		//沿第0轴重复1000次，第1轴重复一次。列数不变，行数重复1000
